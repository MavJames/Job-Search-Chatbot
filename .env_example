# Copy to .env and replace placeholder values

# Azure OpenAI configuration (preferred)
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_DEPLOYMENT=
AZURE_OPENAI_API_VERSION=2025-01-01-preview
AZURE_OPENAI_EMBEDDINGS_DEPLOYMENT=text-embedding-3-small

# OpenAI fallback
# OPENAI_API_KEY=

# LinkedIn credentials for job scraping (if required)
LINKEDIN_COOKIE=
LINKEDIN_USERNAME=
LINKEDIN_PASSWORD=

# Optional encryption key override (32+ bytes recommended)
COOKIE_ENCRYPTION_KEY=

# MCP transport configuration
HEADLESS=true
TRANSPORT=stdio
HOST=127.0.0.1
PORT=8000

# Filesystem MCP server root (defaults to repo root if unset)
FILESYSTEM_ROOT=
